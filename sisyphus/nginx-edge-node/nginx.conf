user www-data;
worker_processes auto;
pid /run/nginx.pid;

events {
    worker_connections 1024;
}

http {

    ##
    # Basic settings
    ##
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    ##
    # Logging
    ##
    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log warn;

    ##
    # Real IP from Cloudflare
    ##
    real_ip_header CF-Connecting-IP;
    set_real_ip_from 0.0.0.0/0;

    ##
    # Proxy defaults (shared)
    ##
    proxy_http_version 1.1;
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;

    proxy_set_header Upgrade           $http_upgrade;
    proxy_set_header Connection        "upgrade";

    proxy_buffering off;
    proxy_read_timeout 300;

    # DEFAULT / FALLBACK (optional)
    server {
        listen 80 default_server;
        server_name _;
        return 444;
    }

    server {
        listen 80;
        server_name wigiwee.com www.wigiwee.com;

        root /var/www/wigiwee.com;
        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }

    server {
        listen 80;
        server_name immich.wigiwee.com;

        location / {
            proxy_pass http://192.168.1.3:2283;
        }
    }

    # ##
    # # APP 2 — example: Node.js / React / Next.js
    # ##
    # server {
    #     listen 80;
    #     server_name app2.mydomain.com;

    #     location / {
    #         proxy_pass http://192.168.1.11:3000;
    #     }
    # }

    # ##
    # # INTERNAL DASHBOARD — example: Grafana
    # ##
    # server {
    #     listen 80;
    #     server_name grafana.mydomain.com;

    #     location / {
    #         proxy_pass http://192.168.1.12:3000;
    #     }
    # }
}
