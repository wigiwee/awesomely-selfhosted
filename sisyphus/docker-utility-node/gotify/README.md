# Gotify - a notification pushing service

## how to set it up

### step 1 - start the container
```bash
docker compose up -d
```
### step 2 - configure the service
visit the configured exposed port and login using default credentials username: admin, password: admin